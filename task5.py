"""
Задача: Дана последовательность целых чисел. Необходимо найти ее самую длинную
строго возрастающую подпоследовательность.

Подпоследовательность получается из исходной последовательности путем удаления
некоторых элементов (возможно, нуля) без изменения порядка оставшихся элементов.

Строго возрастающая подпоследовательность - это подпоследовательность, в которой
каждый следующий элемент строго больше предыдущего.

Ответ должен быть напечатан как одно число - длина самой длинной строго возрастающей
подпоследовательности.
"""

sequence = [10, 22, 9, 33, 21, 50, 41, 60, 80, 3, 5, 7, 2, 8, 1]
variants = []

for i in range(len(sequence)):
    a = sequence[i::]
    for j in range(i, len(sequence)):

        if sequence[j] <= sequence[i]:
            a.remove(sequence[j])

    variants.append(len(a))

ans = variants[0]
for i in range(len(variants) - 1):
    if variants[i + 1] > variants[i]:
        ans = variants[i + 1]

print(ans)



